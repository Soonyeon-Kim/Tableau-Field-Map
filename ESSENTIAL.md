# ESSENTIAL — 5분 안에 파악하기

## 이게 뭔가

Tableau 워크북(`.twbx`) 안의 계산식이 **원본 컬럼에서 출발해 어떻게 만들어졌는지**를 그려주는 CLI다. 의존 관계 목록이 아니라 리니지(계보) 파이프라인이고, 그 위에서 **통합·리네임 후보까지 제안**한다.

stdlib만 쓴다. 설치할 것도, API 키도, 인터넷도 필요 없다.

## 5분 실행

```bash
cd <repo 폴더>          # GitHub 저장소명과 로컬 폴더명이 다를 수 있다
PYTHONUTF8=1 python twbmap.py "<워크북>.twbx"
PYTHONUTF8=1 python test_twbmap.py          # 8/8 통과해야 정상
```

`.twbx`와 `fixtures.local.json`(아래 참고)이 없으면 워크북 검사 1건은 건너뛴다.

만들어지는 것 (`out/`, git에 올라가지 않는다):

| 파일 | 용도 |
|---|---|
| `map.html` | 브라우저로 그냥 열면 된다. 단일 파일, CDN 없음 → 사내망·오프라인에서도 열린다 |
| `map.md` | 통합 후보·재사용도·미사용·이름 점검 리포트. git diff로 변경 추적 |
| `map.json` | 위 둘의 원자료 |

`PYTHONUTF8=1`을 빼면 한글에서 `UnicodeEncodeError`가 난다. `python3`이 아니라 `python`이다.

## map.html 쓰는 법

- **왼쪽 목록**: DataSource·Parameter·CustomDimension·CustomMeasure·Sheet·Dashboard 6개 폴더로 묶여 있다.
  CustomDimension/CustomMeasure는 계산식을 Tableau의 role(dimension/measure)로 나눈 것이다. 폴더 제목을 클릭하면 접고 펼친다.
  숫자는 fan-in(이 필드를 참조하는 수식 수). 위쪽이 허브다
- **그래프**: 좌 → 우가 만들어지는 순서(원본→…→대시보드). 휠=확대, 드래그=이동, 노드 클릭=그 필드로 이동
- **`⇄ 역방향` 버튼**: 좌우를 뒤집어 대시보드→시트→계산식→파라미터→원본 순서로 본다.
  대시보드·시트를 왼쪽 목록에서 직접 골라 "이 화면이 어떤 데이터로 만들어졌나"부터 거슬러 올라갈 때 쓴다
- **`전체 맵` 버튼**: 원본컬럼·파라미터·계산식을 한 화면에 그린다(시트·대시보드 제외).
  전체 맵에서 필드를 고르면(그래프 노드·왼쪽 목록·탭 안 필드명 어디서든) **전체 맵이 풀리고 그 필드의 계보 화면**이 된다.
  전체 맵 안에서 위치를 찾는 건 미니맵과 배율 버튼이 맡는다
- **미니맵**(그래프 우상단): 지금 보고 있는 범위가 사각형으로 표시된다. 찍거나 끌면 그 위치로 이동한다.
  옆의 `−` `+` `맞춤` 버튼은 화면 중심을 기준으로 확대·축소한다
- 하류는 기본적으로 계산식까지만 그린다. 시트·대시보드는 오른쪽 위 체크박스로 켠다 — 하류 시트가 아주 많은 원본 컬럼이 있어 켜면 읽기 어려울 수 있다
- **탭 4개**: 리니지 / 통합 후보 / 이름 점검 / 미사용·복잡도

## 워크북별 파일은 커밋하지 않는다

| 파일 | 내용 | 상태 |
|---|---|---|
| `*.twbx` `*.twb` `*.hyper` `*.twbr` | 사내 실적 데이터 내장 | gitignore |
| `descriptions.json` | 필드별 한국어 설명. 실행하면 자동 생성·병합된다 | gitignore |
| `fixtures.local.json` | 기준 워크북 실측값(테스트 픽스처). 스키마는 `test_twbmap.py:test_workbook` | gitignore |
| `NOTES.local.md` | 기준 워크북에 대해 알아낸 것(허브·구조·검수 대상) | gitignore |
| `out/` | 산출물 | gitignore |

**공개 문서(`ESSENTIAL.md`·`CLAUDE.md`·`docs/`)와 코드에는 사내 필드명·시트명·워크북명을 적지 마라.** 예시가 필요하면 `계산식 A`·`원본컬럼 X` 같은 가명을 쓴다.

## 지금 상태 (2026-09-19)

- 파서·분석·HTML·md 전부 동작. 테스트 8/8 통과, **기준 워크북 미해석 참조 0건** — 미해석이 생기면 CLI가 목록을 찍고 종료코드 `2`로 실패한다
- 설명(`descriptions.json`)은 아직 일부만 채워져 있고 전부 초안이다. HTML에서 "설명 초안" 배지로 표시된다
- 검수한 사람이 아직 없다 — `reviewed: true`인 항목이 0개다

## 다음에 할 일

1. **설명 초안 검수** — fan-in 상위부터 사람이 확인하고 `descriptions.json`의 `reviewed`를 `true`로 바꾼다
2. 나머지 설명 채우기 (fan-in 순서대로)
3. **통합 후보 검토** — `map.md`의 유사 쌍 중 가장 명확한 것부터. 카테고리 문자열만 다른 동일 구조는 파라미터 1개로 합칠 수 있다
4. `map.md`의 미사용 후보를 대시보드에서 직접 확인 (파라미터 컨트롤은 시트 의존성에 안 잡힌다)

워크북별 세부 사항은 `NOTES.local.md`(로컬 전용)에 있다.

## 주의

`.twbx`에는 **사내 실적 데이터가 `.hyper`로 내장**되어 있다. `.gitignore`가 막고 있으니 강제로 추가하지 마라. 자세한 규칙은 `CLAUDE.md`.
